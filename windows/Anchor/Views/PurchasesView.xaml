<UserControl
    x:Class="Anchor.Views.PurchasesView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SearchBox" Grid.Column="0" PlaceholderText="Search purchases" TextChanged="SearchBox_TextChanged" />
            <Button x:Name="AddButton" Grid.Column="1" Content="+ Add purchase" Margin="8,0,0,0" Click="AddButton_Click" />
        </Grid>

        <ScrollViewer Grid.Row="1" HorizontalScrollMode="Auto" HorizontalScrollBarVisibility="Auto" VerticalScrollMode="Disabled" Margin="0,0,0,10">
            <StackPanel x:Name="StatusChips" Orientation="Horizontal" Spacing="8" />
        </ScrollViewer>

        <TextBlock x:Name="ResultCount" Grid.Row="2" FontSize="12" Foreground="{StaticResource AnchorMutedBrush}" Margin="0,0,0,10" />

        <ScrollViewer Grid.Row="3">
            <StackPanel x:Name="RowsList" Spacing="8" />
        </ScrollViewer>
    </Grid>
</UserControl>
