<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows10.0.19041.0</TargetFramework>
    <TargetPlatformMinVersion>10.0.17763.0</TargetPlatformMinVersion>
    <RootNamespace>Anchor</RootNamespace>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <UseWinUI>true</UseWinUI>
    <EnableMsixTooling>false</EnableMsixTooling>
    <WindowsPackageType>None</WindowsPackageType>
    <!-- Framework-dependent: .NET itself is still bundled (SelfContained=true) so no
         separate .NET install is needed, but the WindowsAppSDK/WinUI3 native runtime
         is NOT bundled (WindowsAppSDKSelfContained=false) — it resolves against the
         Microsoft-signed "Windows App Runtime" redistributable installed on the
         machine instead. This avoids both the unpackaged self-contained SxS
         resolution problems and the MSIX self-signed-certificate trust problems,
         at the cost of one extra one-time Microsoft-signed installer for the user. -->
    <SelfContained>true</SelfContained>
    <WindowsAppSDKSelfContained>false</WindowsAppSDKSelfContained>
    <RuntimeIdentifiers>win-x64</RuntimeIdentifiers>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <ApplicationIcon>Assets\anchor.ico</ApplicationIcon>
  </PropertyGroup>

  <ItemGroup>
    <Content Include="Assets\anchor.ico" />
    <Content Include="Assets\anchor-logo.png" />
    <Content Include="Assets\profile-photo.jpg" />
    <Content Include="Assets\Fonts\*.otf" />
    <Content Include="Assets\Fonts\*.ttf" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.WindowsAppSDK" Version="1.6.240923002" />
    <PackageReference Include="Microsoft.Windows.SDK.BuildTools" Version="10.0.26100.1742" />
    <PackageReference Include="Microsoft.VCRTForwarders.140" Version="1.1.0" />
    <PackageReference Include="CommunityToolkit.Mvvm" Version="8.3.2" />
  </ItemGroup>

</Project>
