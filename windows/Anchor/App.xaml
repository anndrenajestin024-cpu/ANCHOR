<Application
    x:Class="Anchor.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    StartupUri="MainWindow.xaml">

    <Application.Resources>
        <ResourceDictionary>
            <Color x:Key="AnchorCream">#FFFAF9F5</Color>
            <Color x:Key="AnchorCharcoal">#FF131417</Color>
            <Color x:Key="AnchorPrimary">#FF55565B</Color>
            <Color x:Key="AnchorMuted">#FFA6A7AC</Color>
            <Color x:Key="AnchorSuccess">#FF5E7A4C</Color>
            <Color x:Key="AnchorWarn">#FFD0A050</Color>
            <Color x:Key="AnchorDanger">#FFB85C48</Color>
            <Color x:Key="AnchorOutlineFaint">#FFE4E5E9</Color>

            <SolidColorBrush x:Key="AnchorCreamBrush" Color="{StaticResource AnchorCream}" />
            <SolidColorBrush x:Key="AnchorCharcoalBrush" Color="{StaticResource AnchorCharcoal}" />
            <SolidColorBrush x:Key="AnchorPrimaryBrush" Color="{StaticResource AnchorPrimary}" />
            <SolidColorBrush x:Key="AnchorMutedBrush" Color="{StaticResource AnchorMuted}" />
            <SolidColorBrush x:Key="AnchorSuccessBrush" Color="{StaticResource AnchorSuccess}" />
            <SolidColorBrush x:Key="AnchorWarnBrush" Color="{StaticResource AnchorWarn}" />
            <SolidColorBrush x:Key="AnchorDangerBrush" Color="{StaticResource AnchorDanger}" />
            <SolidColorBrush x:Key="AnchorOutlineFaintBrush" Color="{StaticResource AnchorOutlineFaint}" />

            <!-- Bundled font files (not system fonts) so the typefaces render identically on every
                 machine. pack://siteoforigin is WPF's scheme for loose files that ship next to the
                 exe (Content, not embedded resources) — no package identity required, unlike
                 WinUI3's ms-appx:// which needs an MSIX identity this app doesn't have. -->
            <FontFamily x:Key="GordenFont">pack://siteoforigin:,,,/Assets/Fonts/Gorden.otf#Gorden</FontFamily>
            <FontFamily x:Key="AvianoSerifFont">pack://siteoforigin:,,,/Assets/Fonts/AvianoSerif.otf#Aviano Serif</FontFamily>
            <FontFamily x:Key="AvianoGothicFont">pack://siteoforigin:,,,/Assets/Fonts/AvianoGothic.ttf#Aviano Gothic  3</FontFamily>
            <FontFamily x:Key="GaleyFont">pack://siteoforigin:,,,/Assets/Fonts/Galey-Regular.ttf#Galey Rounded ExtLt</FontFamily>

            <!-- Default font + size lock for every TextBlock/Control: WPF has no
                 "make text bigger" OS text-scale factor the way WinUI3 does, so no
                 extra opt-out is needed to keep sizes exactly what's set here. -->
            <Style TargetType="{x:Type TextBlock}">
                <Setter Property="FontFamily" Value="{StaticResource AvianoGothicFont}" />
            </Style>
            <Style TargetType="{x:Type Control}">
                <Setter Property="FontFamily" Value="{StaticResource AvianoGothicFont}" />
            </Style>
            <Style x:Key="AnchorHeadingTextStyle" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="{StaticResource AvianoSerifFont}" />
            </Style>
            <Style x:Key="AnchorWordmarkTextStyle" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="{StaticResource GordenFont}" />
                <Setter Property="FontWeight" Value="Bold" />
            </Style>

            <Style x:Key="AnchorFieldLabel" TargetType="TextBlock">
                <Setter Property="FontSize" Value="11" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Foreground" Value="{StaticResource AnchorMutedBrush}" />
                <Setter Property="Margin" Value="0,0,0,4" />
            </Style>

            <!-- ============ Control theming ============
                 WPF's default Button/TextBox/ComboBox/CheckBox chrome is plain unstyled
                 gray boxes — none of the rounded/Fluent look WinUI3 gave for free. These
                 templates bring it back in line with the rest of the app's design. -->
            <Color x:Key="AnchorOutline">#FFD2D3D8</Color>
            <Color x:Key="AnchorPrimaryDark">#FF3F4045</Color>
            <SolidColorBrush x:Key="AnchorOutlineBrush" Color="{StaticResource AnchorOutline}" />
            <SolidColorBrush x:Key="AnchorPrimaryDarkBrush" Color="{StaticResource AnchorPrimaryDark}" />

            <Style TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Control}}">
                <Setter Property="Background" Value="{StaticResource AnchorPrimaryBrush}" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="Padding" Value="12,7" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="Center" Margin="{TemplateBinding Padding}" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Opacity" Value="0.88" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Bd" Property="Opacity" Value="0.72" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="Bd" Property="Opacity" Value="0.4" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Round translucent PIN keypad buttons on the lock screen's dark gradient,
                 matching the prototype's rgba(255,255,255,.08) key style exactly. -->
            <Style x:Key="AnchorPinKeyStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                <Setter Property="Background" Value="#14FFFFFF" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="FontSize" Value="20" />
                <Setter Property="BorderBrush" Value="#33FFFFFF" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="17">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#33FFFFFF" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#4DFFFFFF" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource {x:Type Control}}">
                <Setter Property="Background" Value="White" />
                <Setter Property="Foreground" Value="{StaticResource AnchorCharcoalBrush}" />
                <Setter Property="BorderBrush" Value="{StaticResource AnchorOutlineBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Padding" Value="8,6" />
                <Setter Property="CaretBrush" Value="{StaticResource AnchorPrimaryBrush}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                                <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsFocused" Value="True">
                                    <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AnchorPrimaryBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="AnchorComboItemStyle" TargetType="ComboBoxItem">
                <Setter Property="Padding" Value="10,7" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBoxItem">
                            <Border x:Name="Bd" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                <ContentPresenter />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsHighlighted" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{StaticResource AnchorOutlineFaintBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="{x:Type ComboBox}" BasedOn="{StaticResource {x:Type Control}}">
                <Setter Property="Background" Value="White" />
                <Setter Property="Foreground" Value="{StaticResource AnchorCharcoalBrush}" />
                <Setter Property="BorderBrush" Value="{StaticResource AnchorOutlineBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Padding" Value="8,6" />
                <Setter Property="ItemContainerStyle" Value="{StaticResource AnchorComboItemStyle}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBox">
                            <Grid>
                                <ToggleButton x:Name="Toggle" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                              BorderThickness="{TemplateBinding BorderThickness}"
                                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                              Focusable="False" ClickMode="Press">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <Path Grid.Column="1" Data="M0,0 L6,6 L12,0" Stroke="{StaticResource AnchorMutedBrush}" StrokeThickness="1.6"
                                                          Margin="0,0,10,0" VerticalAlignment="Center" />
                                                </Grid>
                                            </Border>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False" Margin="{TemplateBinding Padding}"
                                                  VerticalAlignment="Center" HorizontalAlignment="Left"
                                                  Content="{TemplateBinding SelectionBoxItem}" />
                                <Popup x:Name="Popup" IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                    <Border Background="White" BorderBrush="{StaticResource AnchorOutlineBrush}" BorderThickness="1" CornerRadius="6"
                                            MinWidth="{TemplateBinding ActualWidth}" MaxHeight="260" Margin="0,2,0,0">
                                        <ScrollViewer>
                                            <ItemsPresenter />
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="{x:Type CheckBox}" BasedOn="{StaticResource {x:Type Control}}">
                <Setter Property="Foreground" Value="{StaticResource AnchorCharcoalBrush}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="CheckBox">
                            <StackPanel Orientation="Horizontal">
                                <Border x:Name="Box" Width="18" Height="18" CornerRadius="4" BorderThickness="1.4"
                                        BorderBrush="{StaticResource AnchorOutlineBrush}" Background="White" VerticalAlignment="Center">
                                    <Path x:Name="Check" Data="M2,6 L6,10 L13,2" Stroke="White" StrokeThickness="2"
                                          StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round" Visibility="Collapsed" />
                                </Border>
                                <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center" />
                            </StackPanel>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Box" Property="Background" Value="{StaticResource AnchorPrimaryBrush}" />
                                    <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource AnchorPrimaryBrush}" />
                                    <Setter TargetName="Check" Property="Visibility" Value="Visible" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
